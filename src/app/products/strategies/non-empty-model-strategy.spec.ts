import { NonEmptyModelStrategy } from './non-empty-model-strategy';

describe('NonEmptyModelStrategy', () => {
  it('should create an instance', () => {
    expect(new NonEmptyModelStrategy()).toBeTruthy();
  });
});
