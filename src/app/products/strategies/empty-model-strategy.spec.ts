import { EmptyModelStrategy } from './empty-model-strategy';

describe('EmptyModelStrategy', () => {
  it('should create an instance', () => {
    expect(new EmptyModelStrategy()).toBeTruthy();
  });
});
